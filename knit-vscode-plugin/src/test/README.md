## Tests

Install ts-node (if not already installed):
`npm install -g ts-node`

### To run adjacencytest.ts 

Run the TypeScript file directly:
`ts-node knit-vscode-plugin/src/test/adjacencyTest.ts`

### To run validateAdjacencyVsKnit.ts

Run the TypeScript file directly:
`ts-node knit-vscode-plugin/src/test/validateAdjacencyVsKnit.ts`